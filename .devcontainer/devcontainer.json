{
    "name": "Mini-CRM Fullstack Workspace",
    "dockerComposeFile": "docker-compose.yml",
    "service": "workspace",
    "workspaceFolder": "/workspace",
    "forwardPorts": [
        3000,
        5173,
        5432
    ],
    "postCreateCommand": "npm install -g pnpm && pnpm install"
}